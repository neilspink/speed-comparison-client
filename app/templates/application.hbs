<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Website Speed Comparison (beta v0.1)</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li>{{#link-to 'home'}}Home{{/link-to}}</li>
              {{#unless session.isAuthenticated}}
                <li><a href="#" {{action 'login' }}>Google+ Login</a></li>
              {{else}}
                <li>{{#link-to 'tasks'}}Tasks{{/link-to}}</li>
                <li>{{#link-to 'tasks.new'}}New Task{{/link-to}}</li>
                <li><button {{action 'speed'}} class="btn btn-link" style="text-decoration: none; outline: none;padding-top: 15px; padding-bottom: 15px;">Check Speeds!</button></li>
                <li><a href="#" {{action 'logout'}}>Logout</a></li>
              {{/unless}}
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row">
        <div class="col-md-12">
          {{outlet}}
        </div>
    </div>
</div>